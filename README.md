# Repo Overview

This repository contains lightweight workflow artifacts for sprint/task planning (templates, a sprint index, and agent definitions).

## Documentation

This repo uses **MkDocs**. Source files live in `docs/` and configuration is in `mkdocs.yml`.

### Build locally

```bash
python -m pip install -r requirements-docs.txt
mkdocs serve
```
